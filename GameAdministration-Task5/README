IMPORTANT
->Ausführung auf Windows?
=>Keine Probleme bekannt.

->Ausführung auf Unix?
=>Das Add() Feature wird NICHT funktionieren. Grund: scanner.useDelimiter("\r\n");
=>Ich verwende Windows daher obige Zeile.
=>Auf einem Unix basierten System, wie einem Mac, muss stattdessen useDelimiter("\n") verwendet werden.

->Warum gibt es Auswahl OS nicht als Feature?
=>A) Wir haben am Anfang abgesprochen das Feature Model nachträglich nicht anzupassen
=>B) Das Model lässt sich auf meinem Windowsrechner nach Commit an GIT nicht mehr ändern.
=>Es tut so, erstellt neue Ordner aber unter FeatureHouse wird das Model nach einem Speichern einfach wieder zurückgesetzt ...